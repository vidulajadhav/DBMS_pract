🧩 Step 1: Update your system
sudo apt update
sudo apt upgrade -y

📦 Step 2: Import MongoDB GPG Key

This verifies the packages before installation.

curl -fsSL https://pgp.mongodb.com/server-7.0.asc | \
sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg --dearmor

🏗️ Step 3: Add MongoDB Repository

For Ubuntu 22.04 (jammy).
If you’re using 20.04, replace jammy with focal.

echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] \
https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | \
sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list


Then update:

sudo apt update

📥 Step 4: Install MongoDB
sudo apt install -y mongodb-org

▶️ Step 5: Start MongoDB Service
sudo systemctl start mongod
sudo systemctl enable mongod
sudo systemctl status mongod


✅ You should see “active (running)”.

🧠 Step 6: Run MongoDB Shell

Once installed, run:

mongosh


You’ll enter the MongoDB shell prompt like this:

test>

🧾 Step 7: Run Your Code

Now you can execute your code line by line:

use libraryDB

db.books.insertOne({title:"DBMS", author:"Silberschatz", price:550})
db.books.find().pretty()

🧰 Step 8: Stopping MongoDB (if needed)
sudo systemctl stop mongod

⚡ Step 9: Restart MongoDB
sudo systemctl restart mongod
